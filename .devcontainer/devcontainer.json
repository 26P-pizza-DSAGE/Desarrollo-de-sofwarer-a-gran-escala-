{
  "name": "Flutter Dev Container",
  "image": "instrumentisto/flutter:latest",
  "features": {},
  "postCreateCommand": "sudo mkdir -p /usr/lib/llvm-18/bin && sudo ln -sf /usr/bin/ar /usr/lib/llvm-18/bin/ar && sudo ln -sf /usr/bin/ld.lld /usr/lib/llvm-18/bin/ld.lld && /bin/sh -c 'flutter pub get'",
  "customizations": {
    "vscode": {
      "settings": {
        "editor.formatOnSave": true
      },
      "extensions": [
        "Dart-Code.dart-code",
        "Dart-Code.flutter"
      ]
    }
  }
}